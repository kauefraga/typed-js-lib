/**
 * An util function that takes two numbers and returns the sum of them.
 * @param {number} num1
 * @param {number} num2
 * @returns the sum of the two numbers
*/
export function sum(num1, num2) {
  return num1 + num2;
}
